@page "/rita_inn"

<AuthorizeView>
    <Authorized>
        @if (context.User.IsInRole("Company") || context.User.IsInRole("Admin"))
        {
            <DashboardRedirect />
        }
        else
        {
            <IndexRedirect />
        }
    </Authorized>
</AuthorizeView>

<Login />

@code {

}
